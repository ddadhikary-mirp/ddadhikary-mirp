#include <iostream>
using namespace std;
int main () {
int min = 0,max =120;
int mid,response; 

//possible age >= min
// possible age < max
while (max - min > 1){
mid = (min + max )/2;

cout << "Is your age less than " ; 
cout<< mid << endl;
cin >> respnse ;
if (esponse == 1)
max = mid;
else
min = mid;
}
cout << "age is :" << min << endl ;
return 0;
}

